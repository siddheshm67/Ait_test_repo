server:
  port: 8085

spring:
  application:
    name: HSA-Correspondence-API
    
  datasource:
    password: root
    url: jdbc:mysql://127.0.0.1:3306/hsa_db
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
   
managemnet:
  endpoints:
    web:
      exposure:
        include:'*'  
    
